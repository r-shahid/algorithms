const colorsArr = [
	{ colors: ['blue', 'purple', 'red'] },
	{ colors: ['black', 'orange', 'blue'] },
	{ colors: ['green', 'red', 'blue', 'orange', 'green'] },
];

//nested for loops and if/else
const counterForIf = () => {
    let count = {}
    for (colors of colorsArr) {
        for (color of colors){
            
        }
    }
}